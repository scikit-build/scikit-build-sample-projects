[build-system]
requires = [
    "setuptools>=42",
    "scikit-build>=0.19.1",
    "cmake>=3.18",
    "ninja",
    "cython<3.2.8",  # INVESTIGATION: test Windows dist failure
]
build-backend = "setuptools.build_meta"

[tool.ruff]
lint.ignore = ["PLC0415"]
