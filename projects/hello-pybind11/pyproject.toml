[build-system]
requires = [
    "setuptools",
    "wheel",
    "scikit-build>=0.12",
    "cmake",
    "ninja",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
